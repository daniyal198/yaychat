# YaysApp Nightly Report — 2026-08-05

**Branch:** nightly/2026-08-05 · **PR:** pending · **Commits:** origin/main..HEAD

## Completed tasks
- Add chat link rendering for the M2 mock slice: Indexx ecosystem links as branded Action Cards and non-Indexx links as External Link Warning cards.
- Add focused tests for chat link classification behavior.
- Refresh `mobile/docs/` contracts/inventory for the link-card chat UI surface.

## Features implemented
- Chat link cards: text messages now classify the first `http(s)` URL and render trusted ecosystem Action Cards or External Link Warning cards in the conversation UI.

## Bugs fixed
- Replaced invalid card accessibility semantics during review with accessible text labels for Action Cards and External Link Warning cards.

## Files changed
- `mobile/src/yaychat/screens/chats/linkCards.ts` — URL extraction and trusted-domain classification for chat messages.
- `mobile/src/yaychat/screens/chats/ChatScreens.tsx` — in-bubble Action Card and External Link Warning rendering, plus cleanup of touched lint issues.
- `mobile/src/yaychat/services/mock/db.ts` — mock chat messages that exercise trusted and external link states.
- `mobile/__tests__/chatLinkCards.test.ts` — classifier coverage for trusted links, external links, punctuation trimming, and no-link messages.
- `mobile/docs/yaychat-component-inventory.md` — documented chat link cards.
- `mobile/docs/yaychat-mock-api-contracts.md` — documented link classification/rendering contract.
- `mobile/docs/yaychat-screen-inventory.md` — updated chat inventory with link-card support.

## Review improvements (Codex `gpt-5.4`)
- Chat link cards: reviewer replaced invalid `accessibilityRole="summary"` usage with accessible text semantics, changed copy from "Indexx ecosystem link" to "Trusted ecosystem link", tightened URL trailing-punctuation cleanup, and added bracketed-link edge-case tests.

## Remaining tasks / blockers
- `npm test` is blocked: `jest` is not found because `mobile/node_modules/.bin` is empty and the local `react-native` package is incomplete (`react-native/package.json` cannot be resolved).
- `npm run lint` is blocked for the same install-state reason: `eslint` is not found through npm scripts. Direct feature-local ESLint via `node node_modules/eslint/bin/eslint.js --quiet ...` passed.
- UI-level render coverage for the new cards is still missing; current automated coverage is helper-level because Jest cannot run in this install state.
- Full repo lint has pre-existing failures outside this feature when invoked directly.
- `docs/yaychat-current-chat-audit.md` remains blocked by the hard nightly safety rule limiting writes to `mobile/`, `.nightly/`, and `reports/`.
